---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import Navbar from "../../../components/layout/Navbar.astro";
import Footer from "../../../components/layout/Footer.astro";
import ScrollPortfolio from "../../../components/sections/ScrollPortfolio.tsx";
import Projects from "../../../components/sections/Projects.tsx";
import { t } from "../../../i18n/utils";
import type { Locale } from "../../../i18n/utils";

const locale: Locale = "en";
---

<BaseLayout title={`Projects | ${t("hero.name", locale)}`} locale={locale}>
  <Navbar locale={locale} />
  <main class="pt-16 lg:pt-0">
    <ScrollPortfolio client:load locale={locale} />
    <div id="project-archive">
      <Projects client:visible locale={locale} />
    </div>
  </main>
  <Footer locale={locale} />
</BaseLayout>
